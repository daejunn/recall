@AGENTS.md

# Recall — 프로젝트 컨텍스트

## WHY (모든 설계의 출발점)

사용자는 일주일에 2-3권의 철학·심리학 책을 읽는다.
책을 읽는 순간 떠오르는 인사이트가 무의식엔 남지만,
의식적으로는 다시 꺼낼 수가 없다. 내 사고방식엔 영향을 주는데,
정작 "그 책에서 뭐라 했지?"를 내 입으로 말하지 못한다.
그래서 그 논리로 나 자신조차 설득시키지 못한다.

목적: 한 번 읽어도 그 인사이트가 active memory에 자리잡게 만드는 것.

## 이론적 토대 (기능 추가/수정 시 이 원리에 부합하는지 점검)

1. **Active Recall** — 사용자는 다시 읽지 않고, 머리에서 꺼낸다.
2. **Generation Effect** — AI가 사용자 대신 인사이트를 써주지 않는다.
   사용자가 자기 언어로 생성한 문장만이 기억에 정착한다.
3. **Elaborative Encoding** — 새 정보를 사용자의 기존 경험·지식과 연결.
   AI는 이 연결을 유도하는 역할.
4. **Retrieval Practice with Feedback** — 사용자가 회상을 시도하면,
   AI의 객관적 지식이 그 회상의 정확성을 점검. 격차 인식이 핵심.

## 데이터 모델

- 책 한 권 = 컨테이너. 그 안에 노트들이 시간순으로 쌓임.
- 노트 = 사용자/AI 대화 로그 전체. 노트 자체엔 제목 없음.
- 세션, 모드, 완료 상태 같은 개념 없음. 언제든 추가/수정 가능.

DB 테이블: books → notes → messages (CASCADE 삭제)

## 사용자 행동 흐름

**책 등록:** 제목 입력 → AI가 핵심 개념·주장 정리 → 사용자 수정 가능 → 저장

**노트 작성:**
1. 빈 입력창. 사용자가 먼저 날것의 생각을 던진다.
2. AI가 follow-up 질문으로 받는다.
3. 대화가 이어지며 생각이 깊어짐.

## AI 행동 원칙

- 사용자보다 먼저 답을 주지 않는다. (Generation Effect 보호)
- 질문은 elaboration 유도: "왜 그게 박혔어?", "네 어떤 경험과 연결돼?"
- 객관적 지식은 회상 정확성 점검 때만 — 단정 말고 가능성으로 제시.
- AI는 대화 상대지, 요약 기계가 아님.

## 기술 스택

- Next.js 16 (App Router, proxy.ts for session refresh)
- Supabase (DB + Auth, RLS 활성화)
- Google OAuth (Supabase Auth 통해)
- Anthropic Claude API (책 메타: opus-4-7, 노트 대화: sonnet-4-6)
- Tailwind CSS
- 배포: Vercel

## v0.1 범위

포함: 책 등록/목록/상세, 노트 작성 + AI 대화, 두 기기 동기화
미포함(v0.2): Spaced Repetition, 책 간 연결, 검색/태그, 공유

## 사용자 정보

- 비전공자, 코딩 모름. 한국어로 대화.
- 새 개념·용어 나올 때마다 한 줄 설명.
- "나중을 위한" 추상화 금지. v0.1에 필요한 것만.
- 진행 중 의문이 생기면 멈추고 묻는다. 임의로 결정해서 만들지 않는다.
